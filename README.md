# 🧩 MCC Project

A Flask-based web application for managing and analyzing MCC data.  
This project includes database models, API routes, templates, and static assets.

---

## 🚀 Features
- Flask backend with SQLAlchemy ORM  
- User authentication (if configured)  
- Database migrations using Flask-Migrate  
- Template rendering (Jinja2)  
- Static uploads folder  
- Easy deployment (Render, Tiiny.host, or Docker)